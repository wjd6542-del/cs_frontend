# cs-frontend 환경변수 예시 — 복사해서 .env 로 사용하세요.
#   cp .env.example .env
# (.env 는 .gitignore 로 커밋되지 않습니다)

# 백엔드 API 베이스 URL (끝에 /api 포함 — LoginPage 는 /auth/login 로 호출)
VITE_API_URL=http://localhost:3003/api

# 백엔드 x-api-key (cs-server 의 API_KEY 와 동일해야 함)
VITE_API_KEY=cs-dev-api-key
